import Badge from "@/components/ui/Badge";
import Reveal from "@/components/ui/Reveal";
import CountUp from "@/components/CountUp";
import { missionCopy, missionStats } from "@/lib/home-content";

export default function Mission() {
  return (
    <section id="about" className="relative bg-surface py-28">
      <div className="mx-auto max-w-6xl px-6 lg:px-8">
        <Reveal className="mx-auto max-w-2xl text-center">
          <Badge>{missionCopy.badge}</Badge>
          <h2 className="mt-6 text-3xl font-semibold tracking-tight sm:text-4xl">
            {missionCopy.heading}
            <span className="text-gradient"> {missionCopy.headingAccent}</span>
          </h2>
          <p className="mt-4 text-muted">{missionCopy.paragraph}</p>
        </Reveal>

        <div className="mt-16 grid grid-cols-1 gap-5 sm:grid-cols-3">
          {missionStats.map((stat, i) => (
            <Reveal key={stat.label} delay={i * 100}>
              <div className="card-border flex h-full flex-col items-center gap-2 rounded-2xl p-8 text-center">
                <p className="font-display text-4xl font-semibold text-gradient">
                  <CountUp to={stat.value} suffix={stat.suffix} />
                </p>
                <p className="text-sm text-muted">{stat.label}</p>
              </div>
            </Reveal>
          ))}
        </div>
      </div>
    </section>
  );
}
